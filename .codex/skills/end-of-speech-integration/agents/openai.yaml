display_name: End Of Speech Integration
short_description: Transport/signal-aware EOS integration with strict separation from VAD internals.
default_prompt: Use the end-of-speech-integration skill. Confirm EOS signal mode, choose the nearest baseline provider, restrict edits to EOS provider+factory+config files, keep VAD internals untouched, and run strict validation with --provider.
