display_name: Noise Reduction Integration
short_description: Provider-scoped denoiser integration with packet ordering and fallback safety.
default_prompt: Use the noise-reduction-integration skill. Confirm denoiser runtime and audio assumptions, implement under denoiser provider folder and factory only, preserve DenoisedAudioPacket semantics including fallback behavior, and run strict validation with --provider.
