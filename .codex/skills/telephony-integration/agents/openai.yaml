display_name: Telephony Integration
short_description: Telephony provider integration with webhook, streamer, and codec/resampling correctness.
default_prompt: Use the telephony-integration skill. Identify transport type (WebSocket/SIP/AudioSocket), wire provider folder and telephony factory only, validate ReceiveCall/InboundCall/StatusCallback/Streamer behavior, and run strict validation with --provider.
