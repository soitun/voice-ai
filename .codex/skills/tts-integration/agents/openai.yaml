display_name: TTS Integration
short_description: Transport-aware TTS provider integration with lifecycle and metric correctness.
default_prompt: Use the tts-integration skill. Determine TTS transport mode (WS/SSE/SDK/HTTP), implement provider-specific lifecycle for delta/done/interruption, preserve tts_latency_ms semantics, and run strict validation with --provider.
